specVersion: "v1"
serviceId: "studyling"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "studyling-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
